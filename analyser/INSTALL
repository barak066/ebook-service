Для правильной работы analyser требуется, чтобы у него был доступ к Django (т.к. он использует ту же базу данных, что и серверная часть)
Для этого необходимо, чтобы рядом с папкой analyser лежала папка server.

